# credenciais/kubeconfigs nunca devem ir para o repo (são extraídos para /tmp no WSL)
*kubeconfig*
*.pem
*.key
!scripts/*.sh

# artefatos de SO
.DS_Store
Thumbs.db

# resultados grandes/gerados que preferimos não versionar binário
*.tgz
*.tar.gz

# material de outra equipe (analisado, não redistribuído neste repositório)
/outraequipe/
